# Health OS

Health OS provides a private health workspace for every authenticated Home Assistant user.
Open the app through Ingress; no Health OS PIN is required. Direct network ports are not exposed.

Each Home Assistant identity is mapped by its stable user ID to one local Health OS account.
Names are informational and are never used to merge or authorize accounts.
