{
  "name": "health-os",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.7.0",
  "scarfSettings": {
    "enabled": false
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace health-os-web",
    "build": "npm run build --workspace health-os-web",
    "lint": "npm run lint --workspace health-os-web",
    "test": "npm run test --workspace health-os-web",
    "format": "npm run format --workspace health-os-web"
  }
}
